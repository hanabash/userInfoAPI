const app = require('./app.js')
const request = require('supertest')

describe('getUser', () => {
    it('returns user json and status code 200 if user found with no errors', () => {
        //add mock user set up and test
    
    });

    //add other scenarios, ex: user found but under the age of 21, invalid ObjectId, etc.
});